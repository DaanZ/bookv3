# Pipeline (PDF -> highlighted JSON summary) and the Streamlit ingest apps
openai
langchain_core
langchain_openai
langchain
langchain_community
pypdf
beautifulsoup4
streamlit
python-dotenv
rootpath
spotipy

# Imported by the pipeline but previously unlisted: chunks.py uses numpy, every
# structured-output model is a pydantic BaseModel, hardcover/request.py uses requests.
numpy
pydantic
requests

# Python validates TLS against certifi, not the OS store, so an intercepting proxy or
# antivirus (AVG's Web Shield, on at least one machine here) breaks every API call with
# CERTIFICATE_VERIFY_FAILED. truststore points Python at the OS store instead; it is a
# no-op where there is nothing intercepting.
truststore

# The reader's backend (api/), which serves books/*.json to the React app in web/
fastapi
uvicorn[standard]
python-multipart   # PDF uploads on the library screen
